import React, { useState } from 'react';

function Timer() {

    return (
        <div>
            <h1>0s</h1>
            <button>Start</button>
            <button>Reset</button>
        </div>
    );
}

export default Timer;